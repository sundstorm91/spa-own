export default class Router {
    constructor (routes) {
        this.routes = routes;
    }

     /**
      * Description placeholder
      * @param {string} url
      */
    navigate(url) {

    }

    /**
      * Description placeholder
      * @param {string} url
      */
    parseUrl(url) {

    }
}